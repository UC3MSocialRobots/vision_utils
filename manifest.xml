<package>
  <description brief="vision_utils">

     vision_utils

  </description>
  <author>user</author>
  <license>BSD</license>
  <review status="unreviewed" notes=""/>
  <url>http://ros.org/wiki/vision_utils</url>

  <!-- ROS -->
  <depend package="roscpp"/>
  <depend package="visualization_msgs"/>
  <depend package="image_transport"/>
  <!-- depend package="pcl"/ -->

  <!-- OpenCV -->
  <depend package="cv_bridge"/>
  <depend package="opencv2"/>

  <!-- AD -->
  <depend package="kinect"/> <!-- devices/kinect project -->
  <depend package="utils"/>
  <depend package="compressed_rounded_image_transport"/>
  
  <export>
    <rosdoc config="rosdoc.yaml" />
    <cpp cflags="-I${prefix} -I${prefix}/build -I${prefix}/msg_gen/cpp/include"  lflags="-L${prefix}/lib -Wl,-rpath,${prefix}/lib"/>
  </export>

</package>


